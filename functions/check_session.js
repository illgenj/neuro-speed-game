const admin = require("firebase-admin");

// We don't have the service account key here easily, but we can do a mock if needed.
// Wait, the functions directory has access to the default credentials if we run it via the emulator or if we have gcloud auth.
